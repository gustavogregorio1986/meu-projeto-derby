import { type Template } from './templates';
export declare class DependencyOptions {
    ignoreTemplate?: Template;
    constructor(options?: {
        ignoreTemplate: Template;
    });
    static shouldIgnoreTemplate(template: any, options?: {
        ignoreTemplate?: Template;
    }): boolean;
    setIgnoreTemplate(template: any): void;
}
