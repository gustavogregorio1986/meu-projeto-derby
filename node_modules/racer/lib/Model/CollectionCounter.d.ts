export declare class CollectionCounter {
    collections: Record<string, CounterMap>;
    size: number;
    constructor();
    reset(): void;
    get(collectionName: string, id: string): number;
    increment(collectionName: string, id: string): number;
    decrement(collectionName: string, id: string): number;
    toJSON(): {};
}
export declare class CounterMap {
    counts: Record<string, number>;
    size: number;
    constructor();
    increment(key: string): number;
    decrement(key: string): number;
}
