var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ChildModel = exports.RootModel = exports.Model = void 0;
var uuid_1 = require("uuid");
/**
 * Base class for Racer models
 *
 * @typeParam T - Type of data the Model contains
 */
var Model = /** @class */ (function () {
    function Model() {
        this.ChildModel = ChildModel;
    }
    /**
     * Creates a new Racer UUID
     *
     * @returns a new Racer UUID.
     * */
    Model.prototype.id = function () {
        return (0, uuid_1.v4)();
    };
    Model.prototype._child = function () {
        return new ChildModel(this);
    };
    ;
    Model.INITS = [];
    return Model;
}());
exports.Model = Model;
/**
 * RootModel is the model that holds all data and maintains connection info
 */
var RootModel = /** @class */ (function (_super) {
    __extends(RootModel, _super);
    function RootModel(options) {
        if (options === void 0) { options = {}; }
        var _this = _super.call(this) || this;
        _this.root = _this;
        var inits = Model.INITS;
        _this.debug = options.debug || {};
        for (var i = 0; i < inits.length; i++) {
            inits[i](_this, options);
        }
        return _this;
    }
    return RootModel;
}(Model));
exports.RootModel = RootModel;
/**
 * Model for some subset of the data
 *
 * @typeParam T - type of data the ChildModel contains.
 */
var ChildModel = /** @class */ (function (_super) {
    __extends(ChildModel, _super);
    function ChildModel(model) {
        var _this = _super.call(this) || this;
        // Shared properties should be accessed via the root. This makes inheritance
        // cheap and easily extensible
        _this.root = model.root;
        // EventEmitter methods access these properties directly, so they must be
        // inherited manually instead of via the root
        _this._events = model._events;
        _this._maxListeners = model._maxListeners;
        // Properties specific to a child instance
        _this._context = model._context;
        _this._at = model._at;
        _this._pass = model._pass;
        _this._silent = model._silent;
        _this._eventContext = model._eventContext;
        _this._preventCompose = model._preventCompose;
        return _this;
    }
    return ChildModel;
}(Model));
exports.ChildModel = ChildModel;
